import React from "react";

const GameScreen = () => {
  return (
    <div className="text-center mt-10">
      <h2 className="text-2xl font-semibold">♠️ Game Room</h2>
      <p className="mt-2">Players are joining... Get ready!</p>
    </div>
  );
};

export default GameScreen;
